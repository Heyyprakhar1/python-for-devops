📌 Objective

The goal of this assignment is to enhance the Day-04 log analyzer script by making it configurable using:

Environment Variables

Command-Line Arguments

This follows DevOps best practices where configuration is kept outside the code.

🛠️ Features

Reads log level from an environment variable

Takes log file name as a command-line argument

Counts INFO, WARNING, and ERROR log entries

Displays summary on terminal

Writes summary to a file (log_summary.txt)

📂 Project Structure
day-05/
│
├── log_analyzer.py
├── app.log
└── README.md

⚙️ Prerequisites

Python 3 installed

Basic understanding of terminal/command prompt